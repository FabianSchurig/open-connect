# Build outputs
/bin/
/dist/
/target/
**/target/
*.exe
*.test
*.out
coverage.out
coverage.txt
*.profraw
*.profdata

# Editor / OS
.vscode/
.idea/
.DS_Store
*.swp
*~

# Local env
.env
.env.local

# Go workspace caches
/.gocache/

# Generated client code is committed (low-maintenance, single-source-of-truth proto/),
# but transient buf caches are not
.buf-cache/

# Test artifacts
testdata/tmp/
*.log

# Local-only signing keys for `oc-sign` development (see testdata/keys/README.md).
# Private keys must NEVER be committed.
testdata/keys/*.pem

# Rust analyzer
**/.rust-analyzer/
